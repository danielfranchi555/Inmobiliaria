"use client";

import { useRouter } from "next/navigation";
import { useEffect } from "react";

export const PropertyListWrapper = () => {
  const router = useRouter();

  return <div>PropertyListWrapper</div>;
};
